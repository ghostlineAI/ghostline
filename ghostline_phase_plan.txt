
GhostLine — Comprehensive Phase Blueprint (v5)
==============================================
Domain        : https://ghostline.ai   (registered at GoDaddy; DNS will delegate to AWS Route 53)
Date          : 2025‑06‑26

Purpose
-------
Build **GhostLine**, a production‑grade, cloud‑hosted ghost‑writing platform that ingests an author’s source materials and, through an iterative multi‑agent workflow, produces a publishable book (≥ 80 pages) that faithfully matches the author’s voice, tone and story arc.

────────────────────────────────────────────────────────
PHASE 0 — Charter & GitHub Org Bootstrapping
Motivation Align stakeholders and establish collaboration scaffolding.
Tasks
  0.1 Create private GitHub org **ghostline** with repos: **web, api, agents, infra, docs**.  
  0.2 Add CODEOWNERS, PR template, default branch protection.  
  0.3 Commit `PROJECT_CHARTER.md` (personas, KPIs ≥ 80‑page book, voice‑similarity ≥ 0.88).  
  0.4 Commit `ADR‑0000.md` (AWS choice, token billing, multi‑agent workflow rationale).  
Artifacts/Done‑When  
  Charter and ADR merged to `main`; CI markdown‑lint passes.

────────────────────────────────────────────────────────
PHASE 1 — Open‑Source Capability Scan
Motivation Select best‑of‑breed open components and ensure licence compliance.
Tasks
  1.1 Cursor script: scrape GitHub/HuggingFace for LangGraph, AutoGen, DSPy, Llama‑Index, CrewAI, etc.  
  1.2 Benchmark GPT‑4o, Claude 3 Haiku, Mixtral‑8x22B: latency (ms) & $/1 k tokens for a 1‑page draft.  
  1.3 Generate `/docs/oss_scan.csv` (licence, stars, last_commit, fit_score, latency, cost).  
  1.4 Create `ADR‑0001.md` documenting accepted stack and explicit rejections.  
Artifacts/Done‑When  
  ADR‑0001 merged; oss_scan.csv shows zero AGPL/viral licences.

────────────────────────────────────────────────────────
PHASE 2 — High‑Level Architecture Blueprint
Motivation Freeze system boundaries before code lands.
Tasks
  2.1 Create Mermaid diagram: Frontend ↔ API ↔ Agent‑Orchestrator ↔ Data plane.  
  2.2 Document data‑flow: upload → ingestion → chunk/embedding → agent DAG.  
  2.3 Select default compute (ECS Fargate); capture trade‑off in `ADR‑0002.md`.  
Artifacts/Done‑When  
  Diagram and ADR‑0002 approved in repo.

────────────────────────────────────────────────────────
PHASE 3 — AWS Landing Zone
Motivation Provision secure, multi‑account AWS foundation.
Tasks
  3.1 Terraform modules: AWS Organizations (dev, staging, prod), three‑tier VPC, centralized logging account.  
  3.2 Enable AWS WAF, Shield Advanced, GuardDuty.  
  3.3 Create encrypted S3 bucket for Terraform state with DynamoDB lock table.  
  3.4 Configure AWS Budgets alarms (email/SNS) for monthly Bedrock and S3 spend thresholds.  
  3.5 Encrypt all S3 buckets with KMS CMKs; block public access org‑wide.  
  3.6 Create initial on‑call runbook `/docs/runbooks/oncall.md` and PagerDuty webhook stub.  
Artifacts/Done‑When  
  `terraform apply` succeeds in **dev**; budget alarms visible in console.

────────────────────────────────────────────────────────
PHASE 4 — Repo & CI/CD Scaffolding (+ Competitor UI Audit)
Motivation Seed codebases and define front‑end direction.
Tasks
  4.1 Initialise monorepo workspaces or separate repos as decided.  
  4.2 Conduct competitor UI audit (Scrivener Cloud, Atticus, Jasper, Sudowrite, Reedsy); store screenshots and pros/cons → `/docs/ui_audit.pdf`.  
  4.3 Decide **buy template** vs **build Tailwind + shadcn**; record decision in `ADR‑0003.md`.  
  4.4 Scaffold chosen frontend (template import *or* Next.js 14 + Tailwind + shadcn).  
  4.5 Scaffold FastAPI 0.111 service, Dockerfiles, Poetry workspaces.  
  4.6 Add GitHub Actions: lint → tests → build Docker → push to Amazon ECR.  
Success Criteria  
  Placeholder landing page builds; Lighthouse perf/accessibility ≥ 90.  
Artifacts/Done‑When  
  ui_audit.pdf and ADR‑0003 merged; CI pipeline green.
────────────────────────────────────────────────────────
PHASE 4.5
Set up ECS and redis

PHASE 4.6
Please review the status of all 5 repos now.  Please understand where we're at with everything.  For this Phase, we will be focusing on databases.

Given the UX we've described thus far, please set up all the database we'll need for this project along with the schemas for them.  Please be sure to integrate and double check that these databases interact properly with all our codebases.

PHASE 4.6b
Finish up DB migration

PHASE 4.7
Rework the entire UX

PHASE 4.8
Make sure all the databases seamlessy integreate with the new UX now

PHASE 4.9
Please review the status of all 5 repos now.  Please understand where we're at with everything.  For this Phase, we will be focusing on copy, digital assets, creative consistency.

Please generate our app logo.  Also, please generate our theme and creative consistency for everything.  I'm feeling something purple and white for the logo maybe>  Like the ghost dissapears it's tail into a pen or a scribbe or something like that.  Please make it modern and techy looking.  Place all these digital assets somewhere, because we will be using them throughout this project.  Create a broad set of assets we'll need for the product, not just the icon.  

Once we've come up with the digital asset strategy along with the assets, let us update dev.ghostline.ai to reflect these.


PHASE 4.10

Start implementing API endpoints
Set up Celery workers for background tasks
Configure Redis for caching
Begin testing the full application flow
The migration process completed successfully, and your database schema is fully deployed in the VPC! 🎉

Ensure that all CI/CD is working properly.  Any push to the repos automatically updates the webapp, automatically updates whatever containers, automatically updates whatever else so that the CI/CD is 100% automated for anytime we rebuild any container anywhere or update the webapp or update databases...everything should be CI/CD.

Also, ensure that the repos on ECR,ECS are part of this.  I'm actually not sure what their purpose is - but I won't ever work with them directly.  I'll just be pushing to the normal GH.  Then hopefully those changes push to ECR,ECS repos if need be.

After completing this, please modify and update the CURRENT_SETUP.md document to reflect how everything is working for these pieces.

────────────────────────────────────────────────────────
PHASE 5 — Authentication & Tenant Model
Motivation Secure login and map each user to billing context.
Tasks
  5.1 Configure Amazon Cognito with Google SSO plus email/password fallback.  
  5.2 Integrate NextAuth or AWS Amplify in frontend.  
  5.3 API JWT middleware creates `users` row `{id, email, plan, token_balance = plan_default}` on first login.  
  5.4 Send welcome and password‑reset emails via Amazon SES.  Make the emails consistent with the overall theme and assets we've set up. 

────────────────────────────────────────────────────────
PHASE 6 — Project & Data‑Room MVP
Motivation Give authors a place to store and create source material.
Tasks
  6.1 Design schema: `projects`, `assets`, `notes`, `voice_memos`, `asset_versions`, `status` enum.  
  6.2 APIs: `/projects` CRUD, `/assets/presign`, `/notes`, `/voice` (start/stop).  
  6.3 Frontend: drag‑drop uploader, rich‑text editor (TipTap), voice recorder (MediaRecorder).  
  6.4 Upon upload completion set asset `status = PROCESSING`.  
  6.5 Add filters and text search to asset table for quick retrieval.  
Artifacts/Done‑When  
  User flow: create project, upload PDF & image, write note, record voice memo; all appear in Data Room.

────────────────────────────────────────────────────────
PHASE 7 — Ingestion & Embeddings
Motivation Convert raw assets into searchable knowledge base.
Tasks
  7.1 Set up processors: Textract OCR for images/PDF; WhisperX transcription for audio.  
  7.2 Chunk extracted text to ≤ 1 k tokens; generate embeddings via Bedrock Titan or open‑source model.  
  7.3 Store chunks and vectors in pgvector (Aurora) or OpenSearch Serverless.  
  7.4 Expose `/search` API; add minimal search bar in Data Room for QA.  
Artifacts/Done‑When  
  Searching a phrase from the uploaded PDF returns correct chunk in < 2 s.

────────────────────────────────────────────────────────
PHASE 8 — Voice & Style Calibration
Motivation Ensure generated prose matches the author’s unique voice.
Tasks
  8.1 Aggregate corpus from user notes and transcribed voice memos.  
  8.2 Prototype two methods: prompt‑only style tokens vs LoRA fine‑tune (phi‑3‑mini).  
  8.3 Run blind A/B evaluation; target similarity ≥ 0.88 cosine.  
  8.4 Document results and chosen method in `ADR‑0004.md`.  
Artifacts/Done‑When  
  ADR‑0004 merged; evaluator scores meet threshold.

────────────────────────────────────────────────────────
PHASE 9 — Outline & Approval Workflow
Motivation Lock narrative skeleton before expensive drafting.
Tasks
  9.1 PlannerAgent generates hierarchical outline JSON.  
  9.2 Outline editor UI: drag‑reorder, inline comments, approve/revise.  
  9.3 Persist approved outline to project metadata and emit `outline_approved` event.  
Artifacts/Done‑When  
  Author approves outline; workflow advances automatically.

────────────────────────────────────────────────────────
PHASE 10 — Iterative Drafting Loop
Motivation Create manuscript with human feedback in the loop.
Tasks
  10.1 ChapterAgent drafts ≤ 5 k token sections with RAG context.  
  10.2 CriticAgent checks tone & narrative consistency; VoiceValidator runs.  
  10.3 UI diff viewer lets author Accept or Request Changes; feedback merged into next prompt.  
  10.4 Maintain version history and token usage per draft; progress bar and chapter list.  
Artifacts/Done‑When  
  Full manuscript ≥ 80 pages produced; average ≤ 2 draft cycles per chapter.

────────────────────────────────────────────────────────
PHASE 11 — Consistency & Safety QA
Motivation Ship accurate, policy‑compliant manuscript.
Tasks
  11.1 ConsistencyAgent verifies timeline, names, plot arcs.  
  11.2 Hallucination detector with factual RAG grounding.  
  11.3 Safety filter (toxicity, libel) passes.  
  11.4 Run `/guardrails/prompt` API on every chapter before QA flag passes.  
Artifacts/Done‑When  
  QA report shows ≥ 95 % pass rate or issues waived by author.

────────────────────────────────────────────────────────
PHASE 12 — Export & Delivery
Motivation Provide professional book formats.
Tasks
  12.1 Convert manuscript markdown → print‑ready PDF & DOCX (Pandoc or Playwright).  
  12.2 Embed cover, metadata, TOC; version outputs in S3 `books/{project}/v{n}`.  
  12.3 Secure download links in portal and email notification.  
Artifacts/Done‑When  
  Author downloads both formats; files open without errors.

────────────────────────────────────────────────────────
PHASE 13 — Billing & Token Metering
Motivation Monetise usage and control cost.
Tasks
  13.1 Define Stripe plans: Basic (100 k tokens/mo), Premium (500 k), Pro (2 M).  
  13.2 Stripe webhook updates Cognito `token_balance`; ledger table records usage.  
  13.3 Middleware decrements tokens per model or ingestion call; low‑balance alert & top‑up flow.  
Artifacts/Done‑When  
  Token exhaustion blocks generation; top‑up restores service instantly.

────────────────────────────────────────────────────────
PHASE 14 — Real‑Time Notifications
Motivation Keep users informed without page refresh.
Tasks
  14.1 Implement WebSocket/SSE channel via API Gateway or AppSync.  
  14.2 Emit events: job_started, chunk_ready, needs_feedback, export_ready, payment_issue.  
  14.3 Notification inbox and user preferences UI; email templates via SES.  
Artifacts/Done‑When  
  Progress bar live‑updates and email notifications arrive as expected.

────────────────────────────────────────────────────────
PHASE 15 — Observability, Backup & Security Hardening
Motivation Operate confidently and protect user IP.
Tasks
  15.1 Instrument services with OpenTelemetry → AWS X‑Ray; build CloudWatch dashboards & alarms.  
  15.2 Daily automated snapshots of RDS and S3; encryption verified.  
  15.3 IAM Access Analyzer, AWS Inspector scans; harden least‑privilege roles.  
  15.4 Execute quarterly DB + S3 restore drill; document results.  
  15.5 Schedule annual external pentest; track and remediate findings.  
Artifacts/Done‑When  
  Restore drill completes < 30 min; pentest report closed; dashboards green.

────────────────────────────────────────────────────────
PHASE 16 — Performance & Load Testing
Motivation Validate scalability and UX under load.
Tasks
  16.1 Run Locust load test across all APIs.  
  16.2 Generate k6 script simulating 1 k concurrent users uploading 2 GB and building a book.  
  16.3 Lighthouse perf check on every page.  
  16.4 Optimise slow endpoints and DB queries until P95 latency < 1.5 s.  
Artifacts/Done‑When  
  All perf targets met; Lighthouse scores ≥ 90.

────────────────────────────────────────────────────────
PHASE 17 — Beta Launch
Motivation Exercise the system with real authors before GA.
Tasks
  17.1 Enable feature flag `beta_access`; invite 30 authors.  
  17.2 Collect metrics and weekly qualitative feedback; triage bugs.  
  17.3 Ensure ≥ 5 authors complete full book; conduct post‑mortem reviews.  
Artifacts/Done‑When  
  NPS ≥ 40; critical bugs resolved; readiness checklist green.

────────────────────────────────────────────────────────
PHASE 18 — General Availability
Motivation Public launch and revenue start.
Tasks
  18.1 Remove invite gate; enable Stripe self‑serve onboarding.  
  18.2 Create Route 53 hosted zone and ACM SSL; update GoDaddy NS for ghostline.ai.  
  18.3 Publish public landing page with pricing, FAQ, support, competitor comparison.  
  18.4 Enable WAF rules, backups, monitoring; run GA launch checklist.  
  18.5 Release launch blog post and press kit; distribute across mailing list and social channels.  
Artifacts/Done‑When  
  First public payment processed; site stable 72 h.

────────────────────────────────────────────────────────
PHASE 19 — Continuous Improvement
Motivation Iterate features and stay best‑in‑class.
Tasks
  19.1 Run weekly sprints to address top customer feedback.  
  19.2 Research LLM and embedding upgrades; evaluate multilingual support and live collaboration.  
  19.3 Maintain bug backlog < 10 open issues; NPS ≥ 50.  
Artifacts/Done‑When  
  Quarterly roadmap updated; sprint metrics on track.

────────────────────────────────────────────────────────
Cursor Execution Format
----------------------
For each phase, paste the block into Cursor wrapped by:

/phase <N> start  
…tasks exactly as above…  
/phase <N> end

Cursor will create GitHub issues and PRs, run Terraform, push artifacts, and enforce CI gates.

Save this file in the repository root as **ghostline_phase_plan.txt**.
